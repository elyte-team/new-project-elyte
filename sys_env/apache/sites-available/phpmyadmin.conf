<VirtualHost *:80>
	ServerName db.asesoramos.net
RewriteEngine on
RewriteCond %{SERVER_NAME} =db.asesoramos.net
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

<VirtualHost *:443>
	ServerName db.asesoramos.net
	
	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/bd

	<Directory /var/www/bd>
		Options Indexes FollowSymLinks MultiViews
		AllowOverride All
		Order allow,deny
		Allow from all
		Require all granted
	</Directory>

	ErrorLog ${APACHE_LOG_DIR}/phpmyadmin-error.log
	CustomLog ${APACHE_LOG_DIR}/phpmyadmin-access.log combined

SSLCertificateFile /etc/letsencrypt/live/asesoramos.net/fullchain.pem
SSLCertificateKeyFile /etc/letsencrypt/live/asesoramos.net/privkey.pem

<IfModule mod_headers.c>
         <FilesMatch "\.(ico|pdf|flv|jpg|jpeg|png|gif|js|css|swf)$">
         Header set Cache-Control "max-age=10024, public"
         </FilesMatch>
</IfModule>

</VirtualHost>
